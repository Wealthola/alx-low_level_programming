#include<stdio.h>
/**
 * main - print the alphabet in lowercase, folllowed by a new line
 * Return: 0
 */
int main(void)
{
	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
	{
		put char(ch);
	}
	putchar('\n');
	return (0);
}
