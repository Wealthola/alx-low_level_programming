#include "main.h"
/**
 * print_last_digit - return the value of the last digit
 * @n: integer as argument
 * Return: return int
 */
int print_last_digit(int n)
{
	int reminder;

	reminder = n % 10;
	return (reminder);
}
